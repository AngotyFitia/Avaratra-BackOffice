@page
@model Avaratra.BackOffice.Pages_Admin_TypeMesures.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeMesure[0].intitule)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TypeMesure[0].description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TypeMesure[0].etat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TypeMesure) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.intitule)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.etat)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.idTypeMesure">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.idTypeMesure">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.idTypeMesure">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
